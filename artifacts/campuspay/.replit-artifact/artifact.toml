kind = "web"
previewPath = "/"
title = "CampusPay"
version = "1.0.0"
id = "artifacts/campuspay"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22041

[services.development]
run = "pnpm --filter @workspace/campuspay run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/campuspay", "run", "build" ]
publicDir = "artifacts/campuspay/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22041"
BASE_PATH = "/"
